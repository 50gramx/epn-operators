apiVersion: v1
kind: Service
metadata:
  name: epn-memory-pool
  namespace: epn-system
  labels:
    epn.io/operator: epn.memory.v1
spec:
  selector:
    app: epn-memory-pool
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  type: ClusterIP
